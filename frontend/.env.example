# Leave empty for same-origin API (local Vite proxy or Railway single-service deploy).
# Only set if the API is on a different host, e.g.:
# VITE_API_URL=https://your-api.up.railway.app/api
VITE_API_URL=
